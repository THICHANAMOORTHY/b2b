{
  "name": "circula-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:all": "npm run dev:all --prefix frontend",
    "dev": "npm run dev --prefix frontend",
    "frontend": "npm run dev --prefix frontend",
    "backend": "npm run api --prefix frontend",
    "api": "npm run api --prefix frontend",
    "build": "npm run build --prefix frontend",
    "start": "npm run start --prefix frontend",
    "lint": "npm run lint --prefix frontend",
    "seed": "npm run seed --prefix frontend",
    "match": "npm run match --prefix frontend",
    "test:db": "npm run test:db --prefix frontend",
    "sync:supabase": "cd backend && .\\venv\\Scripts\\python.exe sync_sqlite_to_supabase.py"
  }
}
